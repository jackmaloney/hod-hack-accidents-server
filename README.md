# hod-hack-accidents-server

Report an accident web server.

To use the web server (after installing docker):

`docker build -t lewismarshall/accidents .`
`docker run -d -P lewismarshall/accidents`


